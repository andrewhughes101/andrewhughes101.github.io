language: node_js
node_js:
  - 12.6
before_script:
  - npm install
  - npm i react-mdl --save
  - npm i react-router-dom --save
script:
  - npm run build
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  keep_history: true
  on:
    branch: master